---
galaxy_info:
  author: George Shuklin
  description: Reboots if reboot is pending after apt-get upgrade (dist-upgrade) and waits until host  returns back online.
  license: BSD
  min_ansible_version: 1.9
  platforms:
  - name: Ubuntu
    versions:
    - all
  - name: Debian
    versions:
    - all
  categories:
  - system
dependencies: []
